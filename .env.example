# --- Binance API (放在 Railway Variables) ---
BINANCE_API_KEY=YOUR_KEY
BINANCE_API_SECRET=YOUR_SECRET

# --- 運行參數 ---
DRY_RUN=true              # 先觀察日誌，不實下單
EXIT_ON_AUTH_ERROR=true   # 啟動若 API 驗證失敗直接退出

# --- 交易參數 ---
BASE_EQUITY=20
RISK_PCT=0.02
LEVERAGE=30
TIMEFRAME=5m
MAX_PARALLEL_SYMBOLS=6

# --- 指標 ---
ATR_LEN=14
ATR_MULT_SL=1.5
ATR_MULT_TRAIL=2.0
DONCHIAN_LEN=20
EMA_FAST=50
EMA_SLOW=200

# --- 迴圈 ---
LOOP_SLEEP_SECS=30

# 可選：指定幣對 (不填則自帶主流 + 依成交額補齊)
# SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT,TONUSDT,SUIUSDT


